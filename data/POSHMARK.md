# Poshmark import

Live closet: [@jengerluxuri0us](https://poshmark.com/closet/jengerluxuri0us)

`poshmark-import.example.json` is the merge schema only. Export listings from Poshmark, map them onto that shape, then fold new rows into `products.ts` (`category: "2nd Chance Resale"`). Do not invent photos or prices.
